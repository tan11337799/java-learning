<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twhupup.dao.FruitMapper">
    <select id="findByCondition" parameterType="fruit" resultType="fruit">
        select * from t_fruit where fid=#{fid} and fname=#{fname} and fcount=#{fcount} and price=#{price} and remark=#{remark}
    </select>
 </mapper>